unit: ci

## Done
Unit `build`: Phase 0 panel review, intent.md, ledger, check, tests and fixture, init/uninstall/scan, hooks, skill, README.
User-test round by four Opus agents (adopter, maintainer, red-team, code review); 20 findings fixed, 13 regression tests added.
Session clock: ticks on startup, clear, or a resume 8h+ after the last tick; ceiling reads the highest of clock, handoffs, commit days. Panel-reviewed.
Unit `ci`: CI workflow on 3.9/3.11/3.13 with a line budget and `check --at stop` as the blocking step. Panel-reviewed.
PRs #1 and #2 merged; main protected by ruleset with the three test checks required.
Unit `housekeeping`: this handoff amended, Dependabot (monthly, grouped) for github-actions in .github/dependabot.yml.

## Open
Actions are referenced by tag, not SHA. The panel asked for SHA pins three times; Dependabot keeps tags current but cannot detect a moved tag. Pin when SHAs can be verified.
A handoff carries one `unit:` line; a session with two units names the later one. Convention, not a check.

## Next
Adopt in two repos for two sessions (acceptance criterion 3). Nothing else in this repo until real use produces findings.
