unit: intent-hooks-wording

## Done
Unit `trailers-and-next`: work is attributed by a `Unit: <name>` commit trailer; the block ends with a `next:` line; handoffs are optional and unread. Panel-reviewed (Decision 8).
Unit `second-pass`: second full panel review; window offsets, no-clock fallback to the earliest open unit, session-start survives a broken ledger.
Unit `intent-wording`: timestamps UTC, commit days in the committer's zone.
Units `readme-adopt`, `readme-placement`, `adoption-findings`: adopting in an existing repo, the installed-vs-vendored placement table, findings from the compass trial.
Unit `post-tool-hook`: `dokime post-tool`, a PostToolUse hook (matcher `Bash|Skill`). Silent unless HEAD moved (cached in `.dokime/head`) or a skill loaded; then the block, only when flagged, via `additionalContext`; no `run:` conditions, no `next:`, never echoes tool input. On by default. `init --write=skill` writes the `/dokime` skill as the on-demand entry point. Five-agent panel; no TUI (Decision 9).
Unit `uninstall-skill`: uninstall removes `.claude/skills/dokime/` when the SKILL.md is dokime's own.
Unit `intent-hooks-wording`: invariants name both mid-session hooks (Stop, PostToolUse).
PRs #6, #7, #8, #9 merged. 596 of 600 lines, 51 tests.
Owner decisions: the Stop hook stays (it fires per turn, not on exit); compass PR #42 waits unmerged until the owner says so.
This handoff has no unit of its own: a handoff is a ledger record, and dokime refuses it as evidence.

## Open
Acceptance criterion 4 (3 sessions by the clock) reads 8: the build unit closed inside its ceiling, later scope and container restarts ticked the rest. Reword or record as a miss; owner's call.
compass PR #42 vendors dokime at 4c05164, before the PostToolUse hook and the skill piece. Re-vendor or switch to the installed form before merging.
`check --at tool` is not exposed on the CLI; a poller (compass, if it ever renders the ledger) would need it to avoid running `run:` conditions.
Two commits before Decision 8 carry no trailer and show as `work-without-unit` until the window moves past them.

## Next
Acceptance criterion 3: two sessions in compass with a real unit, then a second repo. No code change in dokime unless real use produces a finding.
